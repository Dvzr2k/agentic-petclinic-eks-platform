apiVersion: v1
kind: ConfigMap
metadata:
  name: customers-service
  labels:
    app.kubernetes.io/name: customers-service
    app.kubernetes.io/part-of: petclinic
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: service
data:
  CONFIG_SERVER_URL: "http://config-server:8888"
  # Dev RDS endpoint (terraform output rds_endpoint in terraform/environments/dev).
  # Prod overlay patches this to the prod RDS endpoint once that environment is deployed.
  SPRING_DATASOURCE_URL: "jdbc:mysql://petclinic-dev-mysql.cbcsis2u4cvm.eu-central-1.rds.amazonaws.com:3306/petclinic"
