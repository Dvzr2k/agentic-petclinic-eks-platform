apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "petclinic-service.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "petclinic-service.labels" . | nindent 4 }}
data:
  {{- include "petclinic-service.configMapData" . | nindent 2 }}
